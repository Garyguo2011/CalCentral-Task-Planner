class Task < ActiveRecord::Base
  attr_accessible :due, :status, :title, :course, :kind, :release, :user_id
  belongs_to :user
  has_many :subtasks

  def RemainingTime () 
    return Time.new(self.due - Time.now)
  end

  def ElapsedTime () 
    return Time.new(Time.now - self.release)
  end

end
