<p id="notice"><%= notice %></p>
<ul>
  <li>Title: <%= @task.title %></li>
  <li>Course: <%= @task.course %></li>
  <li>Kind: <%= @task.kind %></li>
  <li>Relase Date: <%= @task.release %></li>
  <li>Due Date: <%= @task.due %></li>
  <li>Status: <%= @task.status %></li>
</ul>
<%= link_to 'Edit', edit_task_path(@task) %>
<%= button_to 'Delete', task_path(@task), :method => :delete, :confirm => 'Are you sure?'%>
<%= link_to 'Back to task list', tasks_path %>

<%= link_to 'Show Subtask List', task_subtasks_path(:task_id => @task.id)%>

<!-- 
-# in app/views/tasks/show.html.haml

%h2 Details about #{@task.title}

%ul#details
  %li
    Title:
    =@task.title
  %li
    Course:
    =@task.course
  %li
    Kind:
    =@task.kind
  %li
    Relase Date:
    =@task.release
  %li
    Due Date:
    = @task.due.strftime("%B %d, %Y")
  %li
    Status:
    = @task.status

= link_to 'Edit', edit_task_path(@task)
= button_to 'Delete', task_path(@task), :method => :delete, :confirm => 'Are you sure?'
= link_to 'Back to task list', tasks_path
 -->